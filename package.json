{
  "name": "mealgenna-mobile",
  "version": "1.0.0",
  "description": "AI Powered Meal Planning App",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p www && cp index.html app.js style.css www/ && cp -r assets www/ 2>/dev/null || true",
    "cap-copy": "cap copy",
    "cap-sync": "cap sync",
    "cap-open-ios": "cap open ios",
    "cap-open-android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0",
    "@capacitor/ios": "^5.0.0",
    "@capacitor/local-notifications": "^5.0.8",
    "@revenuecat/purchases-capacitor": "^12.0.4"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^5.0.0"
  }
}
